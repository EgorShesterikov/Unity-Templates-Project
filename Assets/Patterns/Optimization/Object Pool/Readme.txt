Определим класс пула, содержащий коллекцию многоразовых объектов. Каждый объект поддерживает "используется", означающий, что он сейчас "жив". Когда лобъект понадобится, вы запрашиваете его из пула и возврращает его. Когда объект больше не нужен, он возвращается в пул.