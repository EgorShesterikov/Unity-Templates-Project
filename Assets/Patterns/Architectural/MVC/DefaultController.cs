namespace MVC
{
    public class DefaultController : Controller
    {
        public DefaultController(View view, Model model, InputHandler input) : base(view, model, input)
        {
        }
    }
}
